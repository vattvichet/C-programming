#include <stdio.h>
int main()
{
    int x, y, z;
    printf("Enter the x :");
    scanf("%d", &x);
    printf("Enter the y :");
    scanf("%d", &y);

    //

    if ()
    {
        if (y > 2)
        {
            z = x + y;
            printf("z is %d \n", z);
        }

        else
            printf("x is %d \n", x);
    }

    return 0;
}